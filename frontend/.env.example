# Frontend Environment Variables
# Copy to .env and fill values

# API URL - only needed for production deployment
# In development, Vite proxy handles /api automatically
VITE_API_URL=http://localhost:3001
